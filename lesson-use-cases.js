// Practical guidance shown only when a subtopic has a meaningful use decision.
export const LESSON_USE_CASES={
  "syntax-expressions": "Use this when deciding whether a piece of code produces a value that can be composed, returned or passed on, or performs an action as a statement.",
  "let-const-var": "Use this when choosing binding mutability and scope in new code, or when reviewing legacy var-based code.",
  "lexical-scope": "Use this when tracing name lookup, shadowing or why a function sees a particular variable.",
  "hoisting-tdz": "Use this when diagnosing before-declaration behaviour or safely refactoring declaration order.",
  "primitive-types": "Use this when choosing value-like data, checking runtime categories or designing serialization boundaries.",
  "primitive-object-identity": "Use this when a bug may come from mutation, reference sharing or identity-based comparison.",
  "null-undefined": "Use this when designing or consuming APIs that distinguish an absent value from an explicitly empty one.",
  "numbers": "Use this when handling numeric precision, rounding, safe-integer limits or calculations that must not silently lose accuracy.",
  "bigint": "Use this when exact integers may exceed Number's safe range, such as counters or identifiers; keep it separate from Number arithmetic.",
  "strings": "Use this when constructing user-facing text, interpolation or multi-line messages while keeping formatting readable.",
  "symbols": "Use this when you need a non-colliding property key or a language protocol hook that should not appear as an ordinary string key.",
  "coercion": "Use this when reviewing boundary input or an operator whose implicit conversion could change the result.",
  "truthy-falsy": "Use this when writing presence checks, while verifying that valid values such as 0, false or an empty string are not accidentally rejected.",
  "equality": "Use this when choosing comparison semantics deliberately, especially for NaN, signed zero, object identity or mixed types.",
  "operators": "Use this when a dense expression has precedence or readability risk and should be simplified or made explicit.",
  "optional-chaining": "Use this when a nested value is legitimately optional and you want a missing branch to produce undefined instead of throwing.",
  "nullish": "Use this when a fallback should apply only to null or undefined, not to valid false, zero or empty-string values.",
  "destructuring": "Use this when extracting a small, known set of fields or iterable values at a function or data boundary.",
  "spread-rest": "Use this when copying or combining shallow data, or when accepting a variable number of arguments without mutating the source.",
  "if-switch": "Use this when selecting between paths based on conditions or a finite set of known cases.",
  "exceptions": "Use this at a recoverable boundary where a failure must move to a caller or cleanup must run, rather than as ordinary control flow.",
  "for-loop": "Use this when you need an index, explicit loop control, a bounded range or an early exit.",
  "while": "Use this when the number of iterations is driven by a changing condition or sentinel rather than a known collection.",
  "break-continue": "Use this when a search should stop early or individual iterations should be skipped, while keeping the control flow obvious.",
  "for-of": "Use this when consuming values from an iterable such as an array, string, Set, Map or generator.",
  "for-in": "Use this when deliberately traversing enumerable property keys, with own-property filtering when inherited keys are not wanted.",
  "function-forms": "Use this when choosing hoisting, naming, conditional creation and the appropriate function boundary for a piece of behaviour.",
  "arrow-functions": "Use this for concise callbacks or closures that should capture lexical this and should not have their own arguments or constructor behaviour.",
  "parameters": "Use this when designing a function API with optional values, defaults or a variable-length argument list.",
  "return": "Use this when making a function's output and early validation paths explicit.",
  "lexical-environment": "Use this when reasoning about free variables, shadowing and the environment captured by a function.",
  "closures": "Use this when a factory, callback or module needs private state that survives after the outer function returns.",
  "closures-loops": "Use this when creating callbacks inside loops and each callback must retain its own iteration value.",
  "this": "Use this when reviewing method extraction, callbacks or APIs whose behaviour depends on the call site.",
  "call-apply-bind": "Use this when adapting a function to a receiver, forwarding arguments or creating a partially applied callback.",
  "recursion": "Use this for naturally nested or divide-and-conquer problems when the base case and depth are bounded and clear.",
  "higher-order": "Use this when a transformation, predicate or strategy should be passed around and reused without duplicating control flow.",
  "object-literals": "Use this for small records, configuration values and keyed data where object identity or methods are useful.",
  "object-methods": "Use this when behaviour belongs with an object's state and method-call receiver semantics are intentional.",
  "property-enumeration": "Use this when copying, serializing or inspecting properties and the distinction between own, inherited, enumerable and symbol keys matters.",
  "descriptors": "Use this when library or platform code needs precise writable, enumerable, configurable or accessor behaviour.",
  "prototype-chain": "Use this when debugging property lookup, legacy inheritance or the difference between an own property and an inherited one.",
  "constructor-prototype": "Use this when working with older constructor-based APIs or explaining how shared prototype methods avoid per-instance copies.",
  "classes": "Use this when identity, invariants and shared behaviour form a coherent object abstraction with a lifecycle.",
  "private-fields": "Use this when callers must interact through methods or accessors so internal state and invariants cannot be bypassed.",
  "inheritance": "Use this when a genuine substitutable relationship exists and derived objects should share or extend base behaviour.",
  "static": "Use this for factories, constants or operations that belong to the class or domain rather than to an individual instance.",
  "getters-setters": "Use this when property syntax should represent derived or validated state without hiding expensive or asynchronous work.",
  "arrays": "Use this for ordered sequences where index access and sequence operations are the main model.",
  "array-methods": "Use this when map, filter, find, some or every expresses the question more clearly than a manual loop.",
  "reduce": "Use this when a sequence genuinely combines into one accumulated result and map or filter would not be clearer.",
  "shallow-copy": "Use this when updating an outer array or object immutably while intentionally sharing or replacing nested identities.",
  "map": "Use this when dynamic keyed data, object keys or insertion order are central and string-only object keys are limiting.",
  "set": "Use this when membership and uniqueness are the primary operations.",
  "weak-collections": "Use this when metadata should follow an object’s lifetime without making the object strongly reachable through the collection.",
  "dates": "Use this when representing timestamps or calendar values, making timezone and ISO-format intent explicit at the boundary.",
  "regexp": "Use this for bounded, well-named text matching or replacement patterns whose purpose can be covered by tests.",
  "iterator-protocol": "Use this when building or consuming generic iteration APIs that should work across arrays, generators and custom collections.",
  "custom-iterable": "Use this when a domain collection should work naturally with for...of, spread and other iterable consumers.",
  "generators": "Use this when a sequence or state machine should produce values lazily and pause between steps.",
  "yield-delegation": "Use this when composing generators while forwarding values, completion and errors from nested iterators.",
  "lazy-iteration": "Use this when data is large, expensive, infinite or stream-like and should not be materialized all at once.",
  "promise-basics": "Use this when representing one asynchronous result and composing dependent success and failure steps.",
  "promise-errors": "Use this when an asynchronous pipeline needs one deliberate rejection boundary and predictable error propagation.",
  "async-await": "Use this when sequential asynchronous code benefits from synchronous-looking control flow and local try/catch.",
  "promise-combinators": "Use this when coordinating independent async operations and the all, any, race or partial-success policy matters.",
  "engine-host": "Use this when separating portable JavaScript semantics from browser, Node or other host APIs.",
  "call-stack": "Use this when tracing synchronous execution, recursion depth or the immediate cause of a thrown error.",
  "event-loop": "Use this when explaining async ordering, responsiveness or why I/O can progress without blocking JavaScript execution.",
  "microtasks": "Use this when exact ordering between Promise callbacks, queueMicrotask and timer tasks affects correctness.",
  "run-to-completion": "Use this when diagnosing UI or server stalls caused by one long synchronous job.",
  "realms": "Use this when working across iframes, workers or isolated globals where constructors and object identity may differ.",
  "es-modules": "Use this when creating explicit module boundaries with static imports, exports and analyzable dependencies.",
  "named-default": "Use this when choosing whether a module exposes a stable set of named capabilities or one primary default value.",
  "dynamic-import": "Use this when loading optional or rarely used code on demand, including route-level code splitting.",
  "live-bindings": "Use this when module consumers must observe an exported binding as it changes rather than receive a one-time copy.",
  "resource-management": "Use this when acquired resources need deterministic cleanup even when work throws or returns early.",
  "typed-arrays": "Use this when working with compact fixed-width numeric data or binary protocols.",
  "arraybuffer-dataview": "Use this when reading or writing bytes with explicit offsets, widths and endianness.",
  "memory-lifecycle": "Use this when investigating allocation, reachability and garbage-collection behaviour rather than guessing from source size.",
  "closure-memory": "Use this when a long-lived callback may retain more state than intended and cause memory growth.",
  "proxy": "Use this when interception of property or function operations is genuinely needed for validation, virtualization or reactive behaviour.",
  "reflect": "Use this when implementing proxy traps or needing explicit, return-value-friendly meta-object operations.",
  "global-this": "Use this when code must access the host global object consistently across scripts, modules, browsers and Node.",
  "intl": "Use this when displaying dates, numbers, lists or pluralized text for a user's locale instead of hand-formatting.",
  "browser-host": "Use this when deciding which behaviour belongs to portable JavaScript and which requires browser Web APIs.",
  "dom-tree": "Use this when querying, creating or updating document structure in response to application state.",
  "dom-batching": "Use this when a browser update performs many DOM, style or layout operations and responsiveness matters.",
  "browser-events": "Use this when coordinating user interaction, propagation, delegation or cleanup of event listeners.",
  "request-animation-frame": "Use this when changing visual state on the next paint and keeping animation work aligned with the browser renderer.",
  "fetch-api": "Use this when making HTTP requests from browser code and explicitly handling response status, body parsing and cancellation.",
  "structured-clone": "Use this when sending data between browser contexts or workers without sharing ordinary object references.",
  "web-workers": "Use this when CPU-heavy work would otherwise block the main thread and can be isolated behind message passing.",
  "web-storage": "Use this for small, user-scoped browser persistence where synchronous storage limits and serialization are acceptable.",
  "websocket": "Use this when a browser needs a long-lived, two-way connection for live updates or interactive protocols.",
  "performance-api": "Use this when measuring real user-visible timings or comparing a change with high-resolution marks and measures.",
  "node-host": "Use this when deciding which code can remain portable and which code should depend on Node's process, filesystem or network capabilities.",
  "node-globals": "Use this when writing Node-specific diagnostics, environment access or global APIs and you need to distinguish them from browser globals.",
  "node-esm-commonjs": "Use this when integrating packages or entry points across Node's ESM and CommonJS module systems.",
  "node-packages": "Use this when defining package boundaries, export maps, executable entry points or resolution rules.",
  "node-event-loop": "Use this when reasoning about Node I/O concurrency, callback ordering and work that keeps a server responsive.",
  "node-dont-block": "Use this when reviewing CPU-heavy, synchronous or unbounded work that could starve every request on a Node process.",
  "node-timers": "Use this when scheduling deferred work in Node, while treating timer phases as ordering hints rather than exact deadlines.",
  "node-events": "Use this when modelling local pub/sub or lifecycle notifications and you can define listener ownership and error handling.",
  "node-fs-sync-async": "Use this when choosing simple synchronous filesystem work for startup or scripts versus non-blocking APIs in long-lived servers.",
  "node-buffer": "Use this when handling bytes, sockets, files or native memory instead of JavaScript strings and objects.",
  "node-buffer-unsafe": "Use this when allocating buffers from untrusted sizes or performance-sensitive paths where initialization and safety trade-offs matter.",
  "node-streams": "Use this when processing data incrementally so large inputs do not need to fit in memory at once.",
  "node-backpressure": "Use this when a producer can outpace a stream consumer and buffered data, memory or latency must stay bounded.",
  "node-http": "Use this when implementing or reviewing the request, response, headers, body and shutdown lifecycle of a Node HTTP service.",
  "node-workers": "Use this when CPU-intensive JavaScript needs parallel execution without blocking the main event loop.",
  "node-child-process": "Use this when an external executable is the right isolation boundary, with explicit argument, exit and resource handling.",
  "node-memory-usage": "Use this when diagnosing process memory growth and you need to compare heap, RSS, external and ArrayBuffer usage.",
  "node-v8-heap": "Use this when investigating V8 heap limits, spaces, garbage collection pressure or a heap snapshot hypothesis.",
  "node-event-loop-util": "Use this when correlating event-loop saturation or delay with CPU, throughput and request latency.",
  "node-async-local-storage": "Use this when carrying small operational context such as trace or request IDs through async call chains.",
  "node-errors": "Use this when defining error boundaries across Node APIs that may throw, reject, callback or emit an error event.",
  "node-os-resources": "Use this when setting concurrency and resource budgets for sockets, descriptors, threads, CPU and native memory.",
  "debug-response-json": "Use this when a response wrapper, parsed body and consumer disagree; inspect the value at the failing boundary before changing the loop.",
  "debug-var-loop-closure": "Use this when callbacks created in a loop return the same unexpected value and the captured binding is suspect.",
  "debug-microtask-order": "Use this when logs or state updates appear out of order across Promise callbacks, microtasks and timers.",
  "debug-unhandled-rejection": "Use this when an async failure disappears, becomes a process-level warning or crosses a missing catch boundary.",
  "debug-stale-closure": "Use this when a callback observes an old state value after the surrounding UI or async operation has changed.",
  "debug-recursion-stack": "Use this when recursion overflows or never finishes and you need to verify base-case convergence and decreasing input.",
  "debug-dom-listener-leak": "Use this when detached DOM state remains reachable or repeated mounting causes listeners and memory to accumulate.",
  "debug-main-thread-jank": "Use this when input, rendering or animation pauses and a long synchronous browser task is a likely cause.",
  "debug-node-event-loop": "Use this when Node latency rises and you need to distinguish event-loop saturation from slow downstream I/O.",
  "debug-node-rss": "Use this when RSS grows while heapUsed looks stable and native, external or Buffer memory may be responsible.",
  "debug-node-backpressure": "Use this when sustained streaming load causes buffered data, memory or latency to grow because a producer ignores pressure.",
  "debug-node-diagnostic-report": "Use this when a production-only Node failure needs a broad runtime snapshot for postmortem analysis."
};
